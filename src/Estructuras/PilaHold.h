#ifndef PILA_HOLD_H
#define PILA_HOLD_H

// Implementacion de pila propia (LIFO) para la pieza en espera (capacidad 1)
// No se permite usar std::stack

class PilaHold {
public:
    PilaHold();
    ~PilaHold();
    
    // Metodos basicos sugeridos
    // void apilar(Pieza p);
    // Pieza desapilar();
    // bool estaVacia() const;
};

#endif
