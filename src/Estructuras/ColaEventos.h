#ifndef COLA_EVENTOS_H
#define COLA_EVENTOS_H

// Implementacion de cola ordenada por tiempo para eventos programados
// No se permite usar std::priority_queue

class ColaEventos {
public:
    ColaEventos();
    ~ColaEventos();
    
    // Metodos basicos sugeridos
    // void insertarEvento(Evento e); // Insertar en posicion correcta
    // Evento extraerFrente();
};

#endif
