#ifndef COLA_PIEZAS_H
#define COLA_PIEZAS_H

// Implementacion de cola propia (FIFO) para gestionar la secuencia de piezas
// No se permite usar std::queue ni std::deque

class ColaPiezas {
public:
    ColaPiezas();
    ~ColaPiezas();
    
    // Metodos basicos sugeridos
    // void encolar(Pieza p);
    // Pieza desencolar();
    // bool estaVacia() const;
};

#endif
